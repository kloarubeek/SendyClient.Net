namespace Sendy.Client.Model
{
    /// <summary>
    /// Used for the campaign's track_opens and track_clicks parameters.
    /// </summary>
    public enum TrackingOption
    {
        Disabled = 0,
        Enabled = 1,
        Anonymous = 2
    }
}
